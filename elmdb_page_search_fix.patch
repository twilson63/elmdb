--- a/c_src/elmdb_nif.c
+++ b/c_src/elmdb_nif.c
@@ -1726,6 +1726,7 @@ static ERL_NIF_TERM elmdb_async_put_new(ErlNifEnv* env, int argc, const ERL_NIF
 static MDB_txn* elmdb_async_get_handler(MDB_txn *txn, OpEntry *op) {
   k_args *args = (k_args*)op->args;
   MDB_val val;
   ERL_NIF_TERM term_val;
   unsigned char *bin;
   int ret;
+  int retry_count = 0;
   
   /* CRITICAL: Async operations must NEVER reuse transactions */
   txn = NULL;
   
+retry_get:
   if((ret = mdb_txn_begin(args->elmdb_dbi->elmdb_env->env, NULL, MDB_RDONLY, &txn)) != MDB_SUCCESS) {
     SEND_ERRNO(op, ret);
     goto done;
   }
   
   ret = mdb_get(txn, args->elmdb_dbi->dbi, &args->key, &val);
+  
+  /* Handle potential race condition with page rebalancing */
+  if (ret == MDB_CORRUPTED && retry_count < 3) {
+    mdb_txn_abort(txn);
+    retry_count++;
+    /* Small delay to let rebalancing complete */
+    enif_mutex_unlock(args->elmdb_dbi->elmdb_env->txn_lock);
+    usleep(1000 * retry_count); /* Progressive backoff: 1ms, 2ms, 3ms */
+    enif_mutex_lock(args->elmdb_dbi->elmdb_env->txn_lock);
+    goto retry_get;
+  }
+  
   if(ret == MDB_SUCCESS) {
     bin = enif_make_new_binary(op->msg_env, val.mv_size, &term_val);
     memcpy(bin, val.mv_data, val.mv_size);
     SEND_OK(op, term_val);
   }
   else {
     SEND_ERRNO(op, ret);
   }
   
  done:
@@ -1532,6 +1548,7 @@ static MDB_txn* elmdb_async_put_handler(MDB_txn *txn, OpEntry *op) {
   kv_args *args = (kv_args*)op->args;
   ElmdbEnv *env = args->elmdb_dbi->elmdb_env;
   int ret;
+  int retry_count = 0;
   
   /* CRITICAL: Async operations must NEVER reuse transactions
    * This prevents mdb_page_touch assertion failures */
@@ -1551,6 +1568,7 @@ static MDB_txn* elmdb_async_put_handler(MDB_txn *txn, OpEntry *op) {
     goto done;
   }
   
+retry_put:
   if((ret = mdb_txn_begin(env->env, NULL, 0, &txn)) != MDB_SUCCESS) {
     /* Queue is full or other error */
     if (ret == MDB_MAP_FULL || ret == EAGAIN) {
@@ -1572,6 +1590,17 @@ static MDB_txn* elmdb_async_put_handler(MDB_txn *txn, OpEntry *op) {
   
   ret = mdb_put(txn, args->elmdb_dbi->dbi, &args->key, &args->val, args->flags);
   
+  /* Handle potential race condition with page rebalancing */
+  if (ret == MDB_CORRUPTED && retry_count < 3) {
+    mdb_txn_abort(txn);
+    retry_count++;
+    /* Release and re-acquire locks for backoff */
+    enif_mutex_unlock(env->txn_lock);
+    usleep(1000 * retry_count); /* Progressive backoff */
+    enif_mutex_lock(env->txn_lock);
+    goto retry_put;
+  }
+  
   if(ret == MDB_SUCCESS) {
     ret = mdb_txn_commit(txn);
     if(ret == MDB_SUCCESS) {
@@ -1793,6 +1822,7 @@ static ERL_NIF_TERM elmdb_async_get(ErlNifEnv* env, int argc, const ERL_NIF_TER
 static MDB_txn* elmdb_async_delete_handler(MDB_txn *txn, OpEntry *op) {
   k_args *args = (k_args*)op->args;
   int ret;
+  int retry_count = 0;
   
   /* CRITICAL: Async operations must NEVER reuse transactions */
   txn = NULL;
   
+retry_delete:
   if((ret = mdb_txn_begin(args->elmdb_dbi->elmdb_env->env, NULL, 0, &txn)) != MDB_SUCCESS) {
     SEND_ERRNO(op, ret);
     goto done;
   }
   
   ret = mdb_del(txn, args->elmdb_dbi->dbi, &args->key, NULL);
+  
+  /* Handle potential race condition with page rebalancing */
+  if (ret == MDB_CORRUPTED && retry_count < 3) {
+    mdb_txn_abort(txn);
+    retry_count++;
+    /* Small delay to let rebalancing complete */
+    usleep(1000 * retry_count); /* Progressive backoff */
+    goto retry_delete;
+  }
+  
   if(ret == MDB_SUCCESS) {
     ret = mdb_txn_commit(txn);
     if(ret == MDB_SUCCESS)